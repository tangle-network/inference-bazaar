{
  "name": "surplus",
  "private": true,
  "version": "0.1.0",
  "description": "Surplus Intelligence — open market for AI inference. Buy discounted inference, sell surplus inference, market-make the spread.",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "demo:mm": "pnpm --filter @surplus/mm-loop demo"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vitest": "^3.0.0"
  },
  "packageManager": "pnpm@10.33.4"
}
