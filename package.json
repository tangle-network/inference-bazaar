{
  "name": "inference-bazaar",
  "private": true,
  "version": "0.1.0",
  "description": "Inference Bazaar — an open market for AI inference. Buy discounted inference, sell spare capacity, market-make the spread.",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "demo:mm": "pnpm --filter @inference-bazaar/mm-loop demo"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "viem": "^2.52.2",
    "vitest": "^3.0.0"
  },
  "packageManager": "pnpm@10.33.4"
}
